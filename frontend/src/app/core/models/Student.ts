export interface Student {
    id: string
    firstName: string
    lastName: string
    email: string
    phone: string
    bio: string
    country: string
    province: string
    city: string
    postalCode: string
    address: string
  }